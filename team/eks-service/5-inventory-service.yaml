apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-service
  namespace: shoply
spec:
  replicas: 2
  selector:
    matchLabels:
      app: inventory-service
  template:
    metadata:
      labels:
        app: inventory-service
    spec:
      containers:
      - name: inventory-service
        image: ghcr.io/incheon-soda/shoply-inventory:otel
        ports:
        - containerPort: 4002
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        envFrom:
        - configMapRef:
            name: db-config
        - secretRef:
            name: db-secret
---
apiVersion: v1
kind: Service
metadata:
  name: inventory-service
  namespace: shoply
spec:
  selector:
    app: inventory-service
  ports:
  - port: 4002
    targetPort: 4002